To run server type `env $(cat *.env | xargs) node index.js` into terminal (Linux only)
*****************************************************************************************

*******************************************************************************************************
!!! DON'T FORGET PROPERLY USE QUOTATION MARK